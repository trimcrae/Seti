"""ANTIPHASE: an achromatic optical fade answered by a mid-infrared rise.

docs/antiphase.md.  ``coupling`` (the detector), ``energy`` (L_IR/L_opt),
``classify`` (natural classes, blend model, verdict ladder), ``null``
(pair/shift nulls, injections), ``pipeline`` (one star end to end),
``acquire`` (runner-only archive access), ``run`` (stages).
"""
