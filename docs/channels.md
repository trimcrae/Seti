# Channel index

One row per search channel: what it asks, what it reads, how to run it, where
its outputs live, and what those outputs currently say. This is the *map*;
the narrative (why each channel exists, what was found and traced to a
systematic, what to do next) is the log in [`STATUS.md`](../STATUS.md), and
the conventions for adding a channel are
[`channel-brief.md`](channel-brief.md).

Verdict strings are quoted verbatim from the committed result file named in
the row. Where a channel writes no `verdict` field, the row says which file
carries the headline instead. "Pending" means the package, tests and workflow
exist but no run has committed results. Every CLI command is
`python -m seti.cli <command>`; every workflow is `workflow_dispatch` unless a
cron is noted. All channels are unit-tested offline under `tests/` (the suite
is network-guarded: see `tests/conftest.py`).

Index compiled 2026-09-01 from the files on `main` (rows for METRONOME, LANTERN, FALLOUT and BAFFLE added 2026-09-06); re-verify a verdict
against the result file before quoting it elsewhere.

## Waste heat and energy budget

| Channel | Question | Data | CLI | Workflow | Doc | Results → current verdict |
|---|---|---|---|---|---|---|
| **WD IR excess** (`acquire`, `sed`, `contamination`, `discriminate`, `stats`) | Dyson waste heat around white dwarfs that survives a five-stage contamination funnel and a debris-disk discriminant | Gaia DR3 × AllWISE × 2MASS | `acquire-run`, `science-run`, `science-blend`, `contamination-budget` | `science.yml`, `science-blend.yml` | `paper/` manuscript | `results/science/blend_followup_summary.json`: 13 isolated, 7 stellar companions, 3 blends; all τ < 0.08 (ordinary debris disks). **0 technosignature** |
| **CENOTAPH** (`cenotaph`) | Cold Dyson shells (T < 100 K): grey dimming with no mid-IR excess whose energy reappears at 60–160 µm | Gaia DR3 GSP-Spec dwarfs, 2MASS, AllWISE, AKARI/FIS, IRAS | `cenotaph --stage {sample,twins,grey,midir,farir,reduce,probe}` | `cenotaph.yml`, `cenotaph-probe.yml`, `cenotaph-recon.yml`, `cenotaph-recon2.yml` | `cenotaph.md` | `results/cenotaph/summary.json`: `closure_but_crowded_beam`, `sample_verdict: PARTIAL_SAMPLE` (3 of 88 parallax shells timed out) |
| **OSSUARY** (`ossuary`) | Warm dust around stars that cannot make it: metal-poor ([Fe/H] < −1) and halo-kinematic stars (S7, terminal state of an unmaintained swarm) | Gaia DR3 GSP-Spec/GSP-Phot joined in-archive to AllWISE + 2MASS | `ossuary` | `ossuary.yml` | `ossuary.md` | **Pending**: `results/ossuary/` does not exist; first run dispatched, nothing committed |
| **EMBER** (`ember`) | Waste heat that switched off: mid-IR excess present in an early epoch and absent decades later (S1) | IRAS (1983), AKARI/IRC (2006–07), WISE (2010), Gaia DR3 for proper-motion propagation | `ember --stage {audit,probe,acquire,analyse}` | `ember.yml` | `ember.md` | `results/ember/summary.json`: `NO_SURVIVOR` — but `with_early_photometry: 0` of 412,914 acquired, so this is acquisition-limited, not a sky statement |
| **VIGIL** (`vigil`) | Mid-IR variable while optically constant: waste heat with a duty cycle (S4), or that duty cycle ceasing | NEOWISE per-epoch W1/W2 (IRSA), Gaia DR3 | `vigil-probe`, `vigil-sweep`, `vigil-vet` | `vigil.yml` | `vigil.md` | `results/vigil/summary.json`: `NO_DATA_REACHED`, all counts 0 |
| **IGNITION** (`ignition`) | An infrared excess being *born*: a monotonic W1/W2 rise sustained ≥ 5 yr on a Gaia-astrometric, kinematically old, 2010-photospheric field star with a flat optical (S61; the reverse of EMBER) | Gaia DR3 × AllWISE (in-archive join), NEOWISE `neowiser_p1bs_psd` per-epoch W1/W2 (IRSA), optional ZTF/ASAS-SN CSV hook | `ignition --stage {probe,sample,acquire,screen,assess,all} --shard i/n` (or `python -m seti.ignition.run`) | `ignition.yml` | `ignition.md` | `results/ignition/summary.json` (+ `summary_<mode>.json` per mode): the acquire block is **fixed and measured on real data**. Run 35653615329's probe walked all four upload rungs and three returned the *same* sentence from IRSA's own TAP — `INTERNAL_SERVER_ERROR: Unimplemented data type: unicodeChar` — so it was never the transport: `Table.from_pandas` on `source_id.astype(str)` serialises as VOTable `unicodeChar`, which IRSA does not implement. `sid` now goes up as `long` (verified on pyvo 1.9.1's own `Table.write(format='votable')` path), non-numeric ids as ASCII `char`, and a service that refuses `long` too gets one downgrade to a 32-bit row index; the id cannot touch the science (rows are assigned locally by exact unit-vector separation). The same run's `sample` step ran **2 h 22 min** over 20 cones without finishing and the acquire matrix never started, so `sample_from_run_id` reuses a prior run's parent (**1 m 48 s** measured) and `fetch_parent`'s `unit_budget_s` stops one stalled tile eating a shard's clock. **Run 35738088082 confirmed the fix on the live service**: every acquire shard logged `upload[pyvo_sync/long]` and `ok=106 zero=0 failed=0 route=upload`, no rung fell through and no 32-bit downgrade fired; run 35740159635's probe agrees (`neowise_upload[pyvo_sync]_2` OK, 7919 rows, `neowise_route_recommended=upload`, `ALL_ROUTES_REACHABLE`). **846 of 846** parents attempted, with rows, and screened, **0 acquire failures** (was 172 of 846 with 314 failures). Verdict `NO_IGNITION_CANDIDATE`, `degraded: []`, 8 of 8 shards; vetoes `NOT_RISING` 780, `IMPULSIVE_SHAPE` 25, `INSUFFICIENT_EPOCHS` 23, `FADING` 17, `SCAN_SYSTEMATIC` 1 sum to 846 exactly, **0 rise candidates**, at 83.8/95.7/99.4% injection recovery for 0.10/0.20/0.40 mag decade ramps. The ensemble zero-point correction ran on real data for the first time and **does** measure an instrumental term — 8 disjoint shards recover the same 44-bin drift curve (pairwise r **+0.970** W1, **+0.989** W2; per-bin scatter 0.4/0.9 mmag against 19/59 mmag), i.e. NEOWISE fades **+0.019** (W1) / **+0.056** mag (W2) per decade — but it is **not** what removed the old 97 faders: the run's own uncorrected control `raw_two_band_fading_5sigma` is **18** against **17** corrected, so `FADING` 97-of-172 -> 17-of-846 is a change of **sample** (the 172 were a broken route's remnant), not of method. A pilot, not an answer. Scale mode `tiles`: **2,047 tiles / 32,451 deg²** of the |b| > 15° sky, **517×** the pilot's 62.8 deg², ~4 × 10⁵ parent stars; sweep run 35740159635 is queued behind the account-wide Actions ceiling, continue it with `resume_run_id=35740159635` at the same shard count (12) |
| **CRADLE** (`cradle`) | The *shattered cradle*: warm debris at the habitable-zone blackbody radius of a MATURE star, above the collisional steady-state maximum — the cell 250 ≤ T_bb ≤ 350 K **and** log(f/f_max) > 3 **and** age > 1 Gyr from ≥ 2 independent indicators, which is empty in the literature (S52); plus the mineralogy discriminant on any Spitzer/IRS spectrum — silicate-featured vs featureless slag (S53) | Gaia DR3 × AllWISE in-archive join over 768 level-3 HEALPix `source_id` ranges (W3 **and** W4 ≥ 5σ, K_s − W1 < 0.2, |b| > 10°), `astrophysical_parameters` (FLAME, GSP-Spec α/Fe, ESP-CS), 2MASS K_s, `vari_rotation_modulation`; IRSA `allwise_p3as_psd` profile fits, NEOWISE, `irs_enhv211`; VizieR known-disk and rotation catalogues; SIMBAD; SFD E(B−V) | `cradle --stage {probe,acquire,screen,ages,assess,all} --shard i/n` (or `python -m seti.cradle.run`) | `cradle.yml` | `cradle.md` | `results/cradle/summary.json` — **measuring, not yet a verdict**. Run 35741356662 (`stage=all`, 8 shards over the 768 level-3 pixels) has shards 0/1/3 querying the ESA mirror; its `probe` job died at 11:05 a.m. EDT on its own offline gate (`pd.to_numeric(errors="ignore")`, removed in the pandas 3 the runner installs) **without one archive call**, so the acquire matrix runs with no measured join shape. Run 35745911273 (`stage=probe`, fixed commit) queued. Suite 30/30 under pandas 2.3.3 and 3.0.6 |
| **SLAG-WD** (`slag`) | The *refinery vector*: a polluted white-dwarf photosphere no natural parcel reproduces. Tier 1, the calibrated posterior-predictive misfit p of the best natural model (meteorite/planetary mixture x condensation fractionation x the three Koester 2009 accretion phases), per object -- the misfit list nobody has published; Tier 2, |z_pair| > 4 on a process-orthogonal pair (Ti/Al, Sc/Ca, Ca/Al, Sr/Ca, Mn/Cr, Ni/Co) while the rest of a >= 5-element panel is natural, sigma_sys = 0.15 dex (S51) | PEWDD (Williams+2024, VizieR `J/A+A/691/A352/pewdd`, 3,547 star-paper rows over TAP) reconciled to **1,576 objects on the sky**; PEWDD's own meteorite compilations (**1,227 measured bodies**) as the natural family and as the injection null; PyllutedWD diffusion-timescale grids; PEWDD's per-star `SinTime*` as the sinking library | `slag --stage {probe,acquire,screen,assess,all} --shard i/n` (or `python -m seti.slag.run`) | `slag.yml` (sharded), `slag-solo.yml` (one job, one runner slot) | `slag.md` | `results/slag/summary.json` (run 35747793625, 2026-09-22): **`MISFIT_LIST_PRODUCED`** — 3,547 rows -> 1,576 objects -> 123 with a >= 5-element panel -> 168 panels calibrated; **4 UNEXPLAINED (p < 0.01), 6 WATCH**; 121 pair tests, 1 exceedance (HS 0209+0832 Ti/Al z = +6.5) **killed** by `REST_OF_PANEL_NOT_NATURAL`; 0 flags fired; **0 candidates survive**. The misfit p is close to uniform over the population (KS 0.130), so the list is about stars rather than about a model that fits nobody. The meteorite calibration separates it: three of the four UNEXPLAINED have a small meteorite p too, while **GALEX J2339-0424** has p = 0.020 with p(meteorite) = 0.98 and worst element **Be at 3.3 sigma** — the known Klein 2021 Be object, recovered blind, a control landing rather than a discovery, with Li/B unmeasured so spallation is untested. Measured meteorites also refute the brief's premise that the six pairs sit inside ~0.3 dex: Ti/Al spans **2.95 dex** across 1,096 real stones against 0.31 across the compiled end-members, Ca/Al 3.63, Mn/Cr 3.21, Ni/Co 3.53 |
| **ISOTHERM** (`isotherm`) | The *shape* of the waste heat: isothermal excess (S5) or discrete temperature steps in geometric progression (S6) | CASSIS Spitzer/IRS low-res spectra; IRSA/VizieR fallbacks | `isotherm --stage {probe,corpus,screen,shape,calibrate,score}` | `isotherm.yml` | `isotherm.md` | `results/isotherm/summary.json`: `no_shape_anomaly_in_corpus`, `archive_verdict: NOT_PROBED`, 5,480 spectra all `INSUFFICIENT_DATA` |
| **Gaia XP anomalies** (`xp`) | BP/RP spectral shapes no normal-stellar locus reconstructs | Gaia DR3 XP spectra | `xp-run` | `xp.yml` | none | `results/xp/<field>/summary.json` (per-field shards, no aggregate). STATUS: 0 credible, channel bounded — broad anomalies are reddened M-dwarf bands, narrow ones are band-edge artefacts |
| **Cluster** (`cluster`) | Does the IR-excess tail over-cluster in 6D against a matched null, as an expanding population would? | Gaia DR3 × AllWISE cones | `cluster-run`, `cluster-aggregate` (the aggregate step is wired to no workflow) | `cluster.yml` | none | `results/cluster/AGGREGATE.json`: `global_over_clustered: false`; single cone p_pos 0.76 |
| **RING** (`ring`) | Rings around the dead (S63): the Osmanov 300–700 K ring-temperature reading (a W1/W2 excess, above the frozen W3/W4 ceiling) over post-biological hosts — white dwarfs, pulsars (with the two pulsar-planet systems as a named vet), Y/late-T dwarfs (a NEOWISE W2 series that switches is a duty cycle) and young free-floating planetary-mass objects (hotter than the 13 M_J cooling ceiling) | Gaia EDR3 WD catalogue × AllWISE × 2MASS (in-archive PM-propagated join; VizieR + upload join / X-Match as fallbacks); ATNF psrcat × AllWISE / CatWISE2020 with offset-position controls; Kirkpatrick+2021 20 pc census + NEOWISE `neowiser_p1bs_psd`; Faherty+2016 | `ring --stage {probe,acquire,screen,assess,all} --leg {wd,pulsar,bd,ffp,all} [--shard i/n]` (or `python -m seti.ring.run`) | `ring.yml` | `ring.md` | `results/ring/summary.json` — see the STATUS.md section for the current verdict |
| **TIDEMARK** (`tidemark`) | Is any channel's anomaly *rate* spatially structured (gradient, edge, age trend) rather than tracing stellar density? | Own Gaia DR3 × AllWISE parent grid plus sibling channels' anomaly catalogues | `tidemark-acquire`, `tidemark-run`, `tidemark-search` | `tidemark.yml` | `tidemark.md` | `results/tidemark/summary.json`: `STRUCTURE_UNVETTED_POPULATION` ("most likely traces the survey rather than the sky"); 7 of 8 input catalogues `NO_PARENT_SAMPLE` |

## Optical time domain

| Channel | Question | Data | CLI | Workflow | Doc | Results → current verdict |
|---|---|---|---|---|---|---|
| **Dimming** (`dimming`, with `secular` and `glint`) | Boyajian-analogue deep dips; slow secular fade (enshrouding); brief achromatic brightening (specular glint) | ZTF g+r, Gaia DR3, NEOWISE and ASAS-SN in characterisation | `dimming-run`, `dimming-vet`, `dimming-characterize` (glint is produced inside `dimming-run`, vetted by `dimming-vet`) | `dimming.yml`, `dimming-sweep.yml`, `dimming-vet.yml`, `dimming-characterize.yml` | none | `results/dimming/AGGREGATE_occurrence_limit.json`: 0 confirmed analogues in 250,862 stars / 116 fields; top fader `ir_fades_reddening_law`. Glint: 15 candidates, all `chromatic_flare`. Exhausted at the ZTF systematics floor |
| **RUST** (`rust`) | Unmaintained decay: per-season photometric *scatter* (second moment) rising secularly (S9) | ZTF g+r via IRSA; Gaia DR3, SIMBAD, NEOWISE for vetting | `rust-sweep`, `rust-vet` | `rust.yml` | `rust.md` | `results/rust/summary.json`: `NO_CANDIDATES` (7 fields, 5,043 stars; "a count, not a limit") |
| **KNELL** (`knell`) | A period that ceased (S32), every non-detection normalised by injection-measured efficiency in that block's own sampling | ZTF g+r via IRSA; Gaia DR3, SIMBAD, VSX, GCVS | `knell-sweep`, `knell-vet`, `knell-cross` | `knell.yml` | `knell.md` | `results/knell/summary.json`: `NO_CANDIDATES` (6 of 8 fields returned data; 1,163 testable stars) |
| **CENTURY** (`century`) | The same three questions as KNELL, RUST and `dimming/secular` on a **107-year** baseline instead of six (S50): a catalogued period that ceased, season scatter rising, a secular fade — each against the star's own per-plate limiting magnitude and blending history, with the Menzel gap (no plates 1954–1970) and its pre/post photometric offset modelled as a **step, never a trend** (Hippke+2016, Lund+2016: that is how KIC 8462852 acquired its "century dimming") | DASCH DR7 (1885–1992; 252,458,490 sources, 23.6 billion measurements) via the `querycat` / `queryexps` / `lightcurve` REST endpoints and the `daschlab` client; AAVSO VSX + GCVS via VizieR for periods; Gaia DR3 for context | `century --stage {probe,targets,acquire,screen,assess,all} --shard i/n` (or `python -m seti.century.run`) | `century.yml` | `century.md` | **Pending**: probe run 35738717013 reached all three DR7 endpoints (`API_REACHED_NO_LIGHTCURVE`) and corrected three schema assumptions (CSV-line wire format; AFLAGS/BFLAGS live in `daschlab/photometry.py`; light curves carry no exposure time) — see `century.md` §6.1. Sweep run **35745660073** (`stages=full`, 4 shards): targets selected — 384 stars, of which 360 bright and only **24 catalogued variables**, plus 77,632 plate exposure times; 4 sweep shards queued 2026-09-22 12:02 EDT; no `summary.json` yet |
| **TOCSIN** (`tocsin`) | Cross-night recurrence of *achromatic* difference-image events (flash and dip) on catalogued nearby stars in the Rubin alert stream (S30) | Rubin/LSST alerts via ALeRCE TAP (Fink as second broker), Gaia DR3 targets | `tocsin-probe`, `tocsin-targets`, `tocsin-screen`, `tocsin-population`, `tocsin-assess` | `tocsin.yml` (**nightly cron**), `tocsin-probe.yml` | `tocsin.md`, `rubin-outage.md` | `results/tocsin/summary.json`: `NO_NEW_DATA` — the broker frontier has been frozen at MJD 61235.4 (2026-07-14) since Rubin's summit evacuation; verdict `SKY_STOPPED` in `results/rubin_outage/` |
| **TOCSIN-ZTF** (`tocsin.ztf_live`) | The same S30 signature on the LIVE ZTF public alert stream (measured live 2026-09-05) while Rubin is dark | ALeRCE ZTF API (numerator), IRSA `ztf_current_meta_sci` quadrant table (denominator) | `tocsin-ztf-probe`, `tocsin-ztf-targets`, `tocsin-ztf-screen`, `tocsin-ztf-assess` | `tocsin-ztf.yml` (nightly) | `tocsin-ztf.md` | Live since 2026-09-05, twice daily: six nights from 2026-07-14 folded (163,768 star-nights, 32 events, one star at `interest`); backfilling toward the stream |
| **TOCSIN alt-feeds** (`tocsin.altfeeds`) | The same signature on non-Rubin feeds while Rubin is off sky | ASAS-SN Sky Patrol v2, ATLAS forced photometry | `tocsin-altfeeds-probe`, `tocsin-altfeeds-census`, `tocsin-altfeeds` | `tocsin-altfeeds.yml` | `tocsin-altfeeds.md`, `substitute-surveys.md` | `results/tocsin_altfeeds/census.json`: `OK` (139,706 targets); `probe.json`: `PARTIAL` (ATLAS and ZTF usable, ASAS-SN down). Walk rebuilt 2026-09-05 with a job deadline, concurrent ATLAS tasks and a per-star walk state (`tocsin-altfeeds.md` §12); ledgers restart from that run |
| **SHROUD** (`shroud`) | Enshrouded, not destroyed (S33): POSS-I sources absent from the modern optical but present in the infrared | SVO `vanish-neowise` / `vanish-possi` **unreachable** (dead host); VizieR `J/AJ/159/8` (127 rows), VizieR `I/284/out` USNO-B1.0 rebuild, CDS X-Match, Gaia DR3, Pan-STARRS DR2 | `shroud --stage {acquire,photometry,analyze}` | `shroud.yml` | `shroud.md` | `VIZIER_FALLBACK` (degraded): 127-object sample, 0 survivors; USNO-B1.0 route live at 12/12 fields / 33,273 rows / 9.425 deg² but `n_poss1_red_only = 0` from a request artefact now fixed. Run 35741075121 in flight |
| **GROWTH** (`growth`) | The growing transit (S57): Kepler-era vs TESS-era transit depth of every re-detected Kepler planet, after the limb-darkening band ratio and Gaia-neighbour dilution, at fixed impact parameter (duration must change as k, not as b) | Exoplanet Archive TAP `cumulative` (KOI), `toi`, `ps` (tic_id join); Gaia DR3 neighbours within one TESS pixel | `growth --stage {probe,acquire,screen,assess}`; **`growth-direct --stage {probe,targets,measure,assess,control,vet}`** | `growth.yml`, **`growth_direct.yml`** | `growth.md` | **`MEASURED_DEPTH_CHANGE_UNRESOLVED` — the candidate was TRACKED DOWN to a TESS blend.** Stage 1 flagged Kepler-718 b at a factor 2.41 in depth. Stage 2 fitted BOTH eras with one fitter (Kepler 13,912 ± 30 ppm over 643 transits; TESS 29,255 ± 1,521 over 87) and stage 3 ran the census and difference image. Four mundane readings were closed by measurement — the KOI depth is right (z = −0.52), no odd-even signature in either era, no neighbour within one pixel can supply the depth (each needs a 293–900 % eclipse), both eras agree when fitted alike. The fifth was the one nobody had measured: the **reduction ensemble** shows raw SAP photometry at 11,196–11,522 ppm against corrected PDCSAP at 29,255–31,984, a factor 2.61 (`sap_minus_pdcsap_z = −15.6`, `PDC_DEEPER_THAN_SAP`), while Kepler's own SAP and PDCSAP agree to 0.14 %. The RAW TESS depth is 0.80 of Kepler's — the right side of one for a bigger aperture — so the 'growth' is the crowding correction dividing it back out. Spread 9,661 ppm (33 %) swamps the bootstrap error and z falls 10.69 → 2.28. Independently, the difference image puts the transit source **29.3 ± 4.2″ from the target at 7.0σ** across 8 sectors, outside the 21″ census radius entirely  **Stage `direct` (S57, in flight as run 35738702139)** skips the TOI join that held stage 1 to 108 of 9,564 KOIs and fits the TESS depth of all **4,725** confirmed/candidate KOIs directly from SPOC/TESS-SPOC/QLP light curves on both SAP and PDCSAP, with a per-planet detectable depth change on every row. A `%.8g` CSV float had truncated 2,686 of 2,993 nine-digit TIC ids, holding coverage to 54 %; ids are now written as integers and a suspect id is re-resolved from the sky before it is ever queried — coverage 54 % → **88 %**, fitted depths per shard 3 → **19**. The stage recovers **Kepler-1520 b** (the disintegrating planet) unprompted and vetoes it on duration. No growth candidate on any row so far |
| **METRONOME** (`metronome`) | Strict clocks in catalogued flare *timing* (S28): a star whose brief brightenings recur with jitter far below rotational quasi-periodicity | Kepler and TESS flare catalogues (Yang & Liu 2019, Pietras+2022, Günther+2020) and rotation / periodic-variable catalogues, all VizieR | `metronome --stage {probe,acquire,screen,assess,redetect}` (or `python -m seti.metronome.run`) | `metronome.yml` | `metronome.md` | **Measured**: run 35652897914 (2026-09-21) read 1,523,888 catalogued flare times and scanned 3,131 stars -> 53 watch / 14 interest / 1 candidate. `population_period` + `few_cycles` then remove 877 scanned stars and 20 of the 68 survivors as window structure (Kepler 372 d year, TESS span/3 grid edge). The one candidate, kepler:5879574 (P = 0.42327 d), is CONFIRMED in its own Kepler photometry (74 re-detected flares, agreement to 2e-5); its photometric period is the open question. The 14 interest stars are all Tu+2022 and 13 recover 0.000 of their catalogued flares. |
| **FORGE** (`forge`) | Hot exozodis read as ~1500 K swarms (S47): per star, a grey Planck body (T in the swarm range, f free) against the nano-grain emissivity family (a < 0.5 µm, β = 1, 2, T at sublimation) over every published H/K/L/N interferometric excess; the observable is the outlier whose N-band excess IS consistent with the Planck extrapolation of its K excess (1 % in K → ~8 % at 10 µm); variability term from repeat epochs; plus a broadband K−W1 / W1−W2 / W2−W3 leg on the sample and the < 30 pc population at ~10× lower sensitivity | Absil+2013 (FLUOR), Ertel+2014 / Absil+2021 (PIONIER), Nuñez+2017 (JouFLU), Ertel+2018/2020 (HOSTS LBTI), Mennesson+2014 / Millan-Gabet+2011 (KIN), Marshall+2016 polarimetry — all VizieR with runtime discovery; embedded seed tables (`data_assets/forge_*.csv`) verified against the archive per row; Simbad, WDS, Gaia DR3 for companions; 2MASS / AllWISE cones; Gaia × 2MASS × AllWISE join for the population | `forge --stage {probe,acquire,screen,assess,all}` (or `python -m seti.forge.run`) | `forge.yml` | `forge.md` | **In flight**: detector gate green on CI 35740042459 (the injected 1500 K swarm is recovered); workflow registered on `main` 2026-09-22 and run [35744731075](https://github.com/trimcrae/Seti/actions/runs/35744731075) (`stage=all`, `skip_population=true`) dispatched. `results/forge/` not yet written. Offline dry run on the embedded seeds (unverified, not a measurement): 5 of 7 headline hosts are `N_UNTESTED`, Fomalhaut is `nano_preferred` at Δχ² = −21.8 — the expected outcome, since small grains win wherever K and N both constrain |
| **ARC** (`arc`) | Superflares above the starspot energy ceiling (S59): ξ = log E_flare − log[B²/8π A_spot^{3/2}] at f = 1, B = 3 kG, spot area from the rotational amplitude marginalised over latitude; ≥ 2 independent flares with ξ > 0 on a companion-free, unblended dwarf | Okamoto+2021, Shibayama+2013, Tu+2022, Yang & Liu 2019, Günther+2020, Davenport 2016 flare tables; McQuillan+2014 / Santos+2021 amplitudes; Berger+2020 parameters; Gaia DR3 cones — all VizieR | `arc --stage {probe,acquire,screen,assess,all}` (or `python -m seti.arc.run`) | `arc.yml` | `arc.md` | `results/arc/summary.json`: **`NO_CEILING_EXCESS`** — and after stage 2 that verdict still stands. Stage 1 (run 35741271294): 190,486 flares, 8,908 stars, **4,206 assessable**, exactly 1 above the conservative ceiling. Stage 2 put that one star on the pixels (run 35744902798): **KIC 9418692** = Gaia DR3 2080152824394749696, Berger+2020 5677.4 K / 1.089 R☉ / 0.957 M☉, amplitude from its own light curve (`Rvar` 3.8425e-4), ξ_conservative **+0.292**, and **5 of 6 tested flares `on_target`** at 0.100–0.266 px (1.0–2.6σ). **It is still not a candidate**, for two independent reasons: re-measuring the same flares from the same light curves moves the energies by **0.16–3.30×** (median 0.55), which puts ξ at **+0.045** and makes the excess smaller than the systematic on the number it is made of; and its veto is **Gaia RUWE 1.5562**, a companion inside ~0.1″ that the centroid cannot see — an M dwarf there is the standard mundane reading. Stage-2 verdict `CEILING_EXCESS_ON_TARGET_BUT_COMPANION_UNRESOLVED`. **The durable result is methodological**: the centroid test put **5 of 58 catalogued flares on a NEIGHBOUR** at 3.9–16.8σ (run 35738785437, commit c751f006) and closed `stellar_params_assumed` on 20 of 21 stars. Both former interest stars settled: KIC 8487271 `flare_on_target` at ξ −0.920, KIC 11507705 `centroid_ambiguous` at ξ −1.240. See `docs/arc.md` §9.5 |

## Spectroscopy and chemistry

| Channel | Question | Data | CLI | Workflow | Doc | Results → current verdict |
|---|---|---|---|---|---|---|
| **Narrow lines** (`spectra`, with `absorb`) | A single unresolved narrow emission line (CW laser) and its absorption-mode analogue | SDSS-DR17 and DESI-DR1 via SPARCL; SIMBAD | `spectra-run --mode {emission,absorption}`, `spectra-triage`, `spectra-confirm` | `spectra.yml`, `spectra-confirm.yml` | none | `results/spectra_triage/summary.json`: 167 survive (112 emission + 55 absorption) after duplicate / known-line / recurrent-wavelength / galaxy cuts. Next: per-exposure persistence |
| **SPECTRA-PERSIST** (`spectra.persist`) | Does the narrow line survive its own exposures? Each of the 167 survivors measured independently in every exposure that built the coadd, against the estimator's own null in the same spectrum and against the inverse-variance stack of those exposures | SDSS-DR17 full `spec-*.fits` (spCFrame HDUs) on the SAS; DESI-DR1 `cframe`/`sky`; SPARCL; SIMBAD; NIST ASD | `python -m seti.spectra.persist --stage {probe,run,reduce,diagnose,control}` | `spectra-persist.yml` | `spectra-persist.md` | `results/spectra_persist/summary.json`: `PERSISTENT_UNIDENTIFIED_LINES_REMAIN` — but **incomplete and uncalibrated** (62 of 141 spectra, `ckpt_version` 2). 6 `ALIVE_persistent_unidentified`, 106 killed by a rest-frame line, 53 `absent_in_exposures` (not yet re-measured against the null) |
| **TAILINGS** (`tailings`) | The sparse chemical anomaly: one element extreme, the other 20–30 quiet, in cool dwarfs where the convective envelope forbids it naturally (S12/S15/S22) | GALAH DR4, APOGEE DR17 via TAP with runtime schema discovery | `tailings`, `tailings-validate` (offline Griffith+2021 Na-star injection) | `tailings.yml`, `tailingslit.yml` | `tailings.md` | `results/tailings/summary.json`: `DEGRADED_SOURCE (...)` + `SPARSE_CANDIDATES_PENDING_REMEASUREMENT`: 1,809 catalogue-level survivors, none a detection until re-measured from the raw spectrum |
| **MIDDEN** (`midden`) | Whitmire & Wright 1980 nuclear-waste signature: Tc / Pm / actinide lines in photospheres that are not AGB or S-type | ESO HARPS/FEROS via ObsCore TAP; VizieR; NIST ASD | `midden --stage {verify-lines,targets,acquire,score,all}`, `midden-deep` | `midden.yml`, `midden-deepdive.yml` | `midden.md` | `results/midden/REPORT.md`: no population-level Tc excess (178 spectra, 99 stars); the one flag, HD 217522, is roAp rare-earth blending and is not claimed |
| **LANTERN** (`lantern`) | A narrow emission line that is present out of secondary eclipse and *vanishes* while the planet is occulted (S28a): a planet-side monochromatic source, across every public JWST exoplanet time series | MAST JWST `x1dints` (NIRSpec, NIRCam, NIRISS, MIRI) × NASA Exoplanet Archive ephemerides | `lantern {probe,inventory,verify,screen,assess,selftest}` (or `python -m seti.lantern.run`) | `lantern.yml` | `lantern.md` | `results/lantern/verify.json`: the reader, the phase labels and the sensitivity are all **proven on real data** (run 35741401724). WASP-18 b NIRISS/SOSS, 2,720 integrations across 6 tables: eclipse 1,451 ppm at 23.1σ, free step 0.0003 d from the predicted ingress (tol 0.020 d), injected vanishing line back at **62.6σ** as a clean `candidate` with its drift null at 0.42σ. WASP-43 b MIRI/LRS, 9,216 across 30 tables: 4,223 ppm at 7.0σ, injected line 10.6σ. The search runs on the **out-minus-in difference spectrum** — the out-of-eclipse spectrum is held at ~1% of the continuum by the extraction's static pixel pattern, and the difference cancels it: 5σ EW limit 8.87e-5 → **1.73e-6 µm** on SOSS (51× deeper), 1.378e-3 → **1.93e-4 µm** on LRS. Re-planned inventory: **467 scheduled exposures, 509 GB** (78 eclipse-class over 43 hosts at 123 GB, 265 transit, 124 unresolved) after level-3 supersession, from 148 hosts; nothing exceeds the 12 GB cap. Screen run **35745941769** in flight |
| **FALLOUT** (`fallout`) | The stable *residue* of fission (S14): the two-humped fission-yield abundance vector ([Nd/Ba] ≫ 0, [Eu/Nd] < 0, [Mo/Zr] > 0) in cool dwarfs, against the best natural s + r mixture in a Teff-matched peer frame | GALAH DR4 allstar (Data Central cloud FITS, the route that worked for TAILINGS); APOGEE DR17 optional | `fallout --stage {probe,acquire,screen,assess,all}` (or `python -m seti.fallout.run`) | `fallout.yml` | `fallout.md` | `results/fallout/summary.json`: `DEGRADED_SOURCE (...); NO_FISSION_PATTERN` — 101,928 dwarfs (20% testable) and 78,344 giants (98% testable), 0 survivors after the vet stage, 29 `UNEXPLAINED_BY_ALL_TEMPLATES` listed; dwarf completeness < 2% at +1 dex Nd, giant 68%. Next: the Pb/Ag/Pd-carrying high-resolution compilations |
| **GRAVE** (`grave`) | The same fission residue as FALLOUT, but in **Earth's own sedimentary record** (S56): does any horizon carry the two-humped fission-yield mass vector — folded against shale, so the ranking is Ru/Rh +4.77 dex > Pd +3.57 > Te +3.03 > Mo +1.43 relative to Nd, with the Gd cliff at −1.68 and **no Hf with the Zr, no Th with the LREE, no U or V with the Mo, no Ir or Pt with the Ru–Rh–Pd** — that no non-negative mixture of twelve natural reservoirs can build; plus refined particulate (PGE in catalyst rather than chondritic proportions, Ta without Nb, W without Sn/Mo/Bi); then an age stack that asks whether a candidate recurs at one stratigraphic level across INDEPENDENT sections, the way the K–Pg iridium does — hypergeometric on sections, **Holm-corrected across the thirteen boundary windows** (FWER 0.05, not the 0.12 an uncorrected per-window 0.01 would have run at); both positive controls survive it (injected K–Pg cluster p_Holm 5.2e-3, chondritic impact layer 7.8e-3) | SGP (`POST sgp-search.io/api/frontend/post-paged`, schema learned at run time — 94 field codes accepted, **114,688 samples**); EarthChem (REST service **404, gone**; ten-rung endpoint ladder recorded); GEOROC/DIGIS via the Göttingen Dataverse as the tephra reference | `grave --stage {probe,acquire,screen,assess,all}` (or `python -m seti.grave.run`) | `grave.yml` | `grave.md` | **MEASURED** (run 35747786123): `DEGRADED_SOURCE (earthchem:NO_DATA_REACHED); NO_FISSION_VECTOR` over **155,304 analyses** (SGP 101,618 + GEOROC 53,686 reference), 63 above threshold, **0 survivors, 0 candidate sections at all 13 boundaries** in 16,408 sections. Threshold ln LR = 555, set by the control population. **The impact positive control could not run: Ir is measured on 33 analyses, Ru on 25, Rh on 29**, so the light peak rests on Mo/Pd/Te and the K-Pg marker is absent from the corpus. 568 refined-particulate records are diffuse (p_Holm = 1.0 at every boundary) and 65 % of `refined_ta` / 25 % of `refined_w` come from drilled core. Coverage-limited, not sensitivity-limited |
| **ISOTOPE** (`isotope`) | ²⁸Si (or ¹²C) purity beyond the database's own most extreme classified X grain with every measured partner (¹²C/¹³C, ¹⁴N/¹⁵N, ²⁶Al/²⁷Al, ⁴⁴Ti/⁴⁸Ti or δ⁴⁴Ca) solar — refined material lacks the supernova package by construction (S46) | Presolar Grain Database SiC release (Stephan+2024, ApJS 270, 27; 20,230 grains) via human URL / wustl crawl / EarthChem Library search / DataCite DOI lookup | `isotope --stage {probe,acquire,screen,assess,all} [--table-url U] [--table-path P]` | `isotope.yml` | `isotope.md` | `results/isotope/summary.json`: **`NO_PURITY_CANDIDATE`** — 20,502 SiC grains reached through the DataCite route (Zenodo record 20317007) after the wustl host refused the runner; 800 X grains set an empirical frontier of δ²⁹Si ≥ −746.5 ‰ and δ³⁰Si ≥ −789.5 ‰; **0 beyond it**, 0 contamination suspects; 12,823 ordinary, 6,041 fractionation, 1,638 without silicon |
| **ULINE** (`uline`) | Rotational line *patterns* of polar industrial fluorine species (NF₃, CHF₃, CH₂F₂, CF₃Cl, CF₂Cl₂, CFCl₃, CF₃CN, COF₂, SO₂F₂, CHClF₂, CF₂) in published unidentified-line lists (S54); CH₃Cl/CH₃F as the natural baseline and veto | Crockett+2014 Orion KL HIFI and He+2008 IRC+10216 U-line tables (VizieR TAP), JPL/CDMS line lists | `uline --stage {probe,acquire,screen,assess}` (or `python -m seti.uline.run`) | `uline.yml` | `uline.md` | `results/uline/summary.json`: **`NO_PATTERN`** — **80 unidentified lines** over two IRC+10216 surveys. Cernicharo+2000 (`J/A+AS/142/181/table3`, 380 lines, 63 unidentified, 47 vetoed, **16 clean**) was **found by the channel's own column census**, not asserted from a paper; He+2008 contributes 377 lines and 17 unidentified. No industrial species has three coincident transitions. Orion KL (`J/ApJ/787/112`) is **measured absent** from VizieR — no table, empty metadata on three mirrors, ReadMe 404. **Both stated limits removed; run 35752177872 (`stage=full`, 2,000 trials) completed every stage and returned `NO_PATTERN`** — **80 distinct U-lines** (63 Cernicharo+2000 + 17 He+2008; the run's own summary said 143 because `J/A+AS/142/181` table2 and table3 are two views of the same 63 rows, proven by identical coincidence frequencies, now deduplicated in code), **22 distinct species x source pairs**, 17 with >=1 coincidence, **one with >=3** (SO2F2 x IRC+10216, 5 features) which failed both gates (rho=0.20 vs 0.30; p_false=0.67 vs 0.01). `targets_unsearchable` is empty — all 11 species searched. The LTE test now runs (the surveys always carried `T(MB)dv` / `Iint`; the regexes missed them). Predictor accuracy measured: SO2 reproduces JPL/CDMS to a **median 2.71 MHz** over 687 matched lines, and zeroing the quartics moves them by a median **623 MHz**. What now binds is the **constants**: all five new species are `FREQUENCY_LIMITED` (x18-x94), and the literature ladder confirmed CF2Cl2's A/B/C from two independent routes but delivered **no trustworthy quartic set** — the CHClF2 candidate's B/C conflict 33-40% with two references, so nothing was promoted. 7 of 10 sources returned NO_TABLE, so the sample is still two surveys of one star. (a) The five uncatalogued species are predicted from a Watson A-reduced asymmetric-top Hamiltonian (`rotor.py`, constants in `data_assets/rotor_constants.yaml`, every block `verify` with a citation), validated on the runner against SO₂/CH₂F₂/COF₂ line by line and refitted so the *constants'* error and the *Hamiltonian's* floor are reported separately; what now binds is their unmeasured quartic constants, reported per pair as `searchability` (CF₂Cl₂ ×9, SO₂F₂ ×10, CF₂ ×6 `DEGRADED`; CHClF₂ ×45, CFCl₃ ×95 `FREQUENCY_LIMITED`) with the remedy named, plus a `hamiltonian_caveat` for SO₂F₂, whose A-reduction is known to fail. A pattern on `verify` constants alone gives `PATTERN_CANDIDATE_VERIFY_CONSTANTS`, never `PATTERN_CANDIDATE`. (b) The LTE test was never a property of the surveys — Cernicharo+2000 carries `T(MB)dv` and He+2008 `Iint`, and the column regexes simply missed them; Crockett+2014 is now sought over the IOP CDN, the article `suppdata`, the CDS ftp mirror and IRSA's HEXOS delivery, and the U-line census asks `TAP_SCHEMA.columns` for U-line phrases rather than inheriting each source's terms. **A veto bug was found and fixed**: matching on atom counts accepted isomers (glycolaldehyde as methyl formate, CH₃NC as CH₃CN, HCCNC/HNCCC as HC₃N), and their frequencies were rejecting U-lines under the target's name — six lines in Cernicharo+2000 were discarded that way, and the clean sample across both surveys rose from 13 to 19 |
| **BAFFLE** (`baffle`) | The zoo-hypothesis warden's fence (S38): a star whose W1 *and* W2 sit ≥ 0.3 mag below the empirical photospheric locus with normal J/H/K_s — the reciprocal shadow of a band-selective screen on the Sun→star line — then patch coherence (top-hat deficit of radius R/d around it), annual-parallax modulation of the patch edge, and the constant-deficit test on the star's own NEOWISE series; plus the fully-opaque limit (bright 2MASS stars with no AllWISE source) and a radio-void search around nearby stars in LoTSS DR2. ETZ and nearby stars flagged with their own denominators | Gaia DR3 × 2MASS × AllWISE via the archive's PM-propagated cross-match (in-archive pre-selection, 0.5 % locus subsample); NEOWISE single exposures (IRSA); LoTSS DR2 (VizieR TAP) | `baffle --stage {probe,acquire,screen,patch,radio,assess,all}` (or `python -m seti.baffle.run`); `python -m seti.baffle.radio --stage {probe,run}` | `baffle.yml`, `baffle-radio.yml`, `bafflelit.yml` | `baffle.md` | `results/baffle/summary.json`: `NO_MIDIR_DEFICIT_SURVIVOR | NO_TRULY_MISSING_COUNTERPART` — acquisition complete (417,589 deficit-track rows = COUNT(*); ~30 M Gaia G < 15 stars with 2MASS + AllWISE screened, 91 % complete to 0.5-mag two-band deficits); 2,055 two-band deficits → 135 after vetoes → 61 fail the G−K_s contamination test → 74 → vet (Gaia 4″, AllWISE `nb/na`, CatWISE/unWISE, 2MASS flags, UKIDSS/VHS K_s) → 9 → patch + final rules (NEOWISE constancy, confusion, degenerate modulation) → **0**. `results/baffle_bright/`: `NO_BRIGHT_MIDIR_DEFICIT_SURVIVOR` (29,080 G < 7.5 stars at 9–25 µm; vet closed the 13 no-detection stars as coverage gaps / artefact regions). `results/baffle_radio/summary.json`: `RADIO_VOIDS_AT_CONTROL_RATE (n_obs=6, n_exp=4.50, p_excess=0.297)` — the whole LoTSS DR2 catalogue (4.3 M sources) around 4,951 Gaia stars within 50 pc; voids at nearby stars occur at the control-position rate. **0 technosignature** in every tier |

## Kinematics and geometry

| Channel | Question | Data | CLI | Workflow | Doc | Results → current verdict |
|---|---|---|---|---|---|---|
| **Astrometric dark companion** (`accel`) | Astrometric acceleration implying a massive dark companion with no luminous counterpart | Gaia DR3 `nss_acceleration_astro`; published compact-companion catalogues | `accel-run`, `accel-xmatch` | `accel.yml`, `accel-xmatch.yml` | none | `results/accel/literature_crossmatch_summary.json`: 8 class-3, 7 already in Shahaf+2023, 1 known BH; the one "novel" is the weakest solution. Reproduces the published catalogue |
| **HERDSMAN** (`herdsman`) | ≥4 unrelated field stars whose integrated orbits converge on one small volume (herding, or a past rendezvous) | Gaia DR3 6D, RV zero-point corrected | `herdsman-fetch`, `herdsman-scan`, `herdsman-reduce` (`herdsman` is the monolithic form) | `herdsman.yml` | `herdsman.md` | `results/herdsman/REPORT.md`: zero candidates in both time directions inside the 11–12 Myr chance-occupancy horizon |
| **HERDSMAN-B** (`herdsman_b`) | Completed assemblies: catalogued clusters that are chemically field-sampled rather than co-natal | Hunt & Reffert membership × Gaia GSP-Phot [M/H]; GALAH DR3 / APOGEE DR17 for the spectroscopic tier | `herdsman-b --stage ...` (incl. `spectro`) | `herdsman-b.yml` | `herdsman.md` §5 | `results/herdsman_b/REPORT.md`: the one formal candidate (Hogg_4) fails member-level vetting as an extinction–metallicity systematic; spectroscopic tier 0/80 |
| **COMPASS** (`compass`) | Spatially coherent patches of aligned binary orbital poles among unrelated neighbours (shared engineered geometry) | Gaia DR3 `nss_two_body_orbit` joined to `gaia_source` | `compass` | `compass.yml` | `compass.md`, `next-question.md` | `results/compass/REPORT.md`: null at every radius 10–100 pc ("also null, decisively"); rests until Gaia DR4 |
| **RELAY** (`relay`) | Intercepting node-to-node beams by geometry (S60): every directed star pair within 100 pc whose transmitter cone (exact angle, spillover and "Earth between the nodes" counted apart) contains Earth at each of five beam widths; which transmitters Breakthrough Listen has pointed at; the published narrowband hits re-cut against a per-pointing drift prior (the unremoved Earth term plus the pair's kinematics, with the small-angle leak as the width) | Gaia DR3 100 pc sample (ESA TAP / VizieR `I/355` / GCNS), BL open-data API (`api/list-targets`, `api/query-files`), SIMBAD `ident`, VizieR hit tables (Enriquez+2017, Price+2020, Margot+2021/2023, …) | `relay -- --stage {probe,targets,geometry,recut,assess,all}` (or `python -m seti.relay.run`) | `relay.yml` | `relay.md` | **Measured** (run 35740854882, 2026-09-22): geometry exact over 264,973 Gaia DR3 stars inside 100 pc, 5/5 beams — 0 qualifying pairs for a 10-m optical link, 18,381 spillover + 145,734 between at 100 m/1.42 GHz, 1.59e6 + 1.46e7 at 10 m/1.42 GHz; fitted θ slopes 1.954 / 2.000. VizieR carries these surveys' target lists and no event list (73 tables, 0 with a drift column), so the hit tables are read from the arXiv e-prints (`papers.py`); full-scale `stage=all` in run 35745111146 |

## Solar-system bodies

| Channel | Question | Data | CLI | Workflow | Doc | Results → current verdict |
|---|---|---|---|---|---|---|
| **DERELICT** (`derelict`) | Dead lightsails: large *radial* non-gravitational acceleration (A1) with no outgassing, normalised by size into an area-to-mass ratio (S19) | JPL SBDB non-gravitational fits; comet control | `derelict --stage ...` (incl. `probe`) | `derelict.yml` | `derelict.md` | `results/derelict/summary.json`: `ALL_SURVIVORS_EXPLAINED`; `completeness.verdict: CONSTRAINT_COMPLETE`. 'Oumuamua recovered as positive control |
| **LOOM** (`loom`) | A *population* of self-replicating probes: cross-object structure in Rubin per-detection ephemeris residuals, gated by the radiation-momentum ceiling | Rubin SSO alerts via ALeRCE; MPC/JPL SBDB orbits | `loom-probe`, `loom-screen`, `loom-assess`, `loom-calibrate`, `loom-litcheck` | `loom.yml` (**weekly cron**), `loom-probe.yml`, `loom-calibrate.yml` (**monthly cron**), `loom-litcheck.yml` | `loom.md` | `results/loom/assessment.json`: `INSUFFICIENT_POPULATION` (66,686 orbits screened; every residual series is one apparition long). Waits for Rubin's second apparition |
| **LOOM catalogue** (`loom.catalogue`) | Over the whole small-body catalogue, how many objects exceed the momentum ceiling: the denominator for any single exceedance | JPL SBDB full catalogue | `loom-catalogue` | `loom-catalogue.yml` (chains `loom-litcheck`) | `loom-catalogue.md` | `results/loom-catalogue/catalogue.json`: `TAIL_DENSELY_POPULATED` (30 of 594 exceed; 7 reliably fitted and unexplained in 116 papers) |
| **SEXTANT** (`sextant`) | Non-gravitational acceleration in Gaia's milliarcsecond minor-planet astrometry: the same question as LOOM at 100× the precision | Gaia DR3 + FPR `sso_observation` / `sso_source` via ESA Gaia TAP | `sextant-probe`; `sextant --stage {probe,fit,assess,all} --shard i/n` | `sextant.yml` (probe -> N fit shards -> assess), `sextant-probe.yml`, `sextantlit.yml` | `sextant.md`, `sextant-priorart.md`, `sextant-acquire.md` | `results/sextant/probe.json`: `OK` (2026-09-03) — schema, service limits and the archive conventions measured (`epoch` is TCB days from JD 2455197.5; state vectors equatorial; `is_rejected` 0.630%); `results/sextant/summary.json` carries the A1/A2/A3 search verdict, scored against the published Yarkovsky detections as positive controls; run **35746692260** (2026-09-22) is the first uncapped fit — `gaiafpr`, all numbered objects, the one-runner `solo` path, controls worked first and a 230-min in-job clock |
| **CRYPT** (`crypt`) | Artifacts in lunar permanent shadow (S55). **Thermal (running):** inside the LOLA mapped PSR ANDed with the Diviner cold-trap definition, a 240 m pixel whose *floor* bolometric temperature — the minimum over every loaded (season, local-time) bin of the Diviner Polar Cumulative Products — exceeds its local annulus by >= 5 sigma while its winter diurnal amplitude and its summer-winter offset stay below threshold. Every passive heating term inside a PSR (rim scattered light, re-radiated terrain IR) has a clock and collapses in winter; an internal source does not. **Radar:** compact Mini-RF CPR >= 1 pixels in a quiet neighbourhood. **Optical:** ShadowCam / NAC coverage listed per survivor. Every prior PSR artifact search is optical ML on NAC/ShadowCam | Diviner PCP local-time products `PCP_AVG_TBOL_POL{N,S}_{SUM,WIN}_LTIM{nn}_240.TAB` (PDS3 ASCII, ~4.4 M rows, 262 MB each) from `urn-nasa-pds-lro_diviner_derived1/data_derived_pcp/diurnal/ltim/`, URLs resolved through ODE; `LPSR_65{N,S}_240M`; Mini-RF mosaics; ODE footprint service | `crypt --stage {probe,pcp,assess_diurnal,all} --shard i/n` (or `python -m seti.crypt.run`) | `crypt.yml` | `crypt.md` | **Running**: 86 offline tests (green under the runner's pandas 3.0.6 / numpy 2.4.6). The probe established that NO per-channel Diviner polar gridded product exists — all 912 PCPs are AVG TBOL — so the multi-channel anisothermality screen is retained but not runnable; the diurnal/seasonal invariance screen is what the archive supports. Run 35747661552 dispatched 2026-09-22 |

## Life around LHS 1140 and K2-18 (origin-of-life fan-out)

| Channel | Question | Data | CLI | Workflow | Doc | Results → current verdict |
|---|---|---|---|---|---|---|
| **Panspermia** (`panspermia`) | Which stars passed close and slow to K2-18 and could carry its material? | Gaia DR3 6D; NASA Exoplanet Archive | `panspermia-run`, `panspermia-mc`, `panspermia-targets`, `panspermia-dossier`, `panspermia-regime` | `panspermia.yml`, `panspermia-mc.yml`, `panspermia-targets.yml`, `panspermia-dossier.yml` | none | `results/panspermia/summary.json`: 15 within 2 pc, closest 0.90 pc at 32 km/s, 0 co-movers. No capturable bridge; RV completeness is the gap |
| **LHS 1140 origin** (`lhs1140_origin`) | Same engine with LHS 1140 b as donor: recipients and directed-travel destinations | Gaia DR3 6D; Exoplanet Archive | `lhs1140-origin` | `lhs1140-origin.yml` | none | `results/lhs1140_origin/summary.json`: 22 recipients, 5,255 destinations, closest 0.26 pc (no verdict field) |
| **Galactic encounters** (`galactic`) | Full-orbit integration back 300 Myr: who passed the biosignature anchors? | Gaia DR3 RV-complete 6D; Exoplanet Archive | `galactic-encounters` | `galactic-encounters.yml` | none | `results/galactic/summary.json`: 0 planet-host encounters, 0 companion flags for both anchors; per-anchor biosignature `answer` fields |
| **ISO back-track** (`iso`) | Do 1I, 2I, 3I back-track toward LHS 1140 or any nearby star? | Published ISO elements; Gaia DR3 | `iso-backtrack` | `iso.yml` | none | `results/iso/summary.json`: `any_consistent_with_origin: false` (all three at ~15 pc) |
| **LHS 1140 deep-dive** (`lhs1140`) | Every techno/bio signature on the star, planets b/c and 38 neighbours, plus a biosignature detectability budget | Gaia (astrometry, XP), WISE/NEOWISE, ZTF, TESS, MAST inventory | `lhs1140` | `lhs1140.yml` | none | `results/lhs1140/summary.json`: `ANOMALY_FLAGGED` on one `companion` flag (RUWE 1.53, marginal binarity); `BIOSIGNATURE_NOT_DETECTABLE_WITH_CURRENT_DATA` |
| **JWST bio** (`jwst_bio`) | Real JWST/HST transmission spectrum of LHS 1140 b: disequilibrium pair, M-dwarf abiotic gate, MIRI eclipse, laser scan | MAST JWST `x1dints` (NIRISS, NIRSpec, MIRI) | `jwst-bio` | `jwst-bio.yml` | none | `results/jwst_bio/summary.json`: `no_biosignature_detected` (2 stacks analysed; MIRI eclipse not measured) |
| **Cross-correlation** (`crosscorr`) | Doppler-resolved O₂ / H₂O in LHS 1140 b's transit | ESO archive (ESPRESSO/HARPS/NIRPS), DACE | `crosscorr` | `crosscorr.yml` | none | `results/crosscorr/summary.json`: `NO_ARCHIVAL_IN_TRANSIT_HIRES_SPECTRA_AVAILABLE` |
| **SETI archive** (`seti_archive`) | Has any targeted radio/optical SETI campaign pointed at LHS 1140, and to what EIRP? | Breakthrough Listen open data, CADC ObsCore | `seti-archive` | `seti-archive.yml` | none | `results/seti_archive/summary.json`: `NO_TARGETED_RADIO_SETI_ON_RECORD` — an observational gap, with representative facility limits |

## Roman Space Telescope (pre-launch intake; built 2026-09-09)

| Channel | Question | Data | CLI | Workflow | Doc | Results → current verdict |
|---|---|---|---|---|---|---|
| **ROMAN intake** (`roman.archive`, `roman.products`, `roman.schema`) | Is any Roman product public yet, and can every reader here consume it? IRSA TAP/SIA, the S3 buckets (OpenUniverse 2024 simulations; the expected flight bucket), MAST, the IPAC simulation pages, `roman_datamodels`; lazy ASDF `dq`-only reads, Level 1 ramps, light-curve / spectrum table adapters with runtime column-role resolution | IRSA, AWS S3, MAST (runner only) | `roman {probe,inventory,ingest,readiness}` (or `python -m seti.roman.run`) | `roman.yml` (**monthly cron** `readiness`; `stage=full` on dispatch) | `roman.md` §1 | `results/roman/readiness.json` (2026-09-09): `data_state: SIMULATIONS_ONLY` — 12/12 endpoints answered; no Roman table at IRSA TAP/SIA, the flight bucket does not exist, MAST CAOM 400; the OpenUniverse 2024 bucket serves SNANA light curves, galsim TDS/WAS images with DQ planes, truth indices and the pointing sequence (`roman.md` §1.4); `selftest.json`: PASS 12/12 on the runner; `alerts.py` opens an issue on MISSION_DATA_PRESENT |
| **ROMAN/lens** (`roman.lens`) | S40, the opaque lens: a microlens opaque over a fraction ρ_L of its Einstein radius removes the minor image in the wings (u > 1/ρ_L − ρ_L), a symmetric pair of steps of depth A₋(u_c); implied density below any bound body | GBTDS Level 4 light curves (F146 + F087); pre-launch: the RMDC26 challenge light curves (Hugging Face `RGES-PIT/*`, W149/Z087) | `roman screen --channel lens` | `roman.yml` | `roman.md` §2.1, §4A | `results/roman/lens/summary.json` (run 7, RMDC26 Beginner tier): `SIMULATION_PACES_OK` — on 133 simulated GBTDS microlensing events 75 `LENSING_NO_OCCULTATION`, 56 `NOT_LENSING`, 2 occulting-pending (to read against the challenge truth); on the 2,688 simulated supernovae of run 4 the time-symmetry gate left 145 pending (edge of coverage) and 2,511 `NOT_LENSING`. Prior art: the occulting-lens morphology is natural-body literature (Agol 2002; arXiv:2608.24009), the density test and the search are not. Pending flight data |
| **ROMAN/lines** (`roman.lines`) | S41, the industrial line: an unresolved emission line on a stellar point source in the 1.00–1.93 µm grism / 0.75–1.80 µm prism, the band holding Nd:YAG 1.064 and Er-fibre 1.55 µm; slitless-contamination vetoes | HLWAS grism, HLTDS prism Level 4 spectra; Euclid NISP as substitute | `roman screen --channel lines` | `roman.yml`, `roman-lit.yml` | `roman.md` §2.2 | **Pending flight data**; no NIR slitless laser search in the record (PANOSETI/NIROSETI are targeted, ground-based) |
| **ROMAN/flash** (`roman.flash`) | S42, the sub-exposure flash: PSF-shaped `JUMP_DET` clusters centred on catalogued stars in Level 2 `dq`, confirmed on the Level 1 ramp (one resultant, no slope change, PSF amplitude map) | GBTDS / HLTDS / HLWAS Level 2 `dq`, Level 1 ramps for candidates | `roman screen --channel flash` | `roman.yml` | `roman.md` §2.3 | `results/roman/flash/summary.json`: `SIMULATION_PACES_OK` on 2 image cutouts with 0 jump pixels — the simulated `DQ` planes are all zero (33.4 M pixels censused), so the jump path waits for flight Level 2 |
| **ROMAN/statite** (`roman.statite`) | S43, the statite: a CGI reflected-light point source whose astrometry is fixed rather than Keplerian; grey, specular | CGI Level 2–4 products | `roman screen --channel statite` | `roman.yml` | `roman.md` §2.4 | **Pending CGI data**; neighbours Jaiswal 2023 (surface glints) and Vides+2019 (WFIRST coronagraph laser SETI) |
| **ROMAN/paces** (`roman.bridge`) | Every existing light-curve / spectrum channel (dips, glint, secular fade, RUST, KNELL, METRONOME, narrow lines) on Roman products with GBTDS-season parameters and the F146/F087 achromaticity test | as above | `roman screen --channel paces` | `roman.yml` | `roman.md` §3, §4A | `results/roman/paces/summary.json` (run 7, RMDC26): every pace ran on 133 GBTDS-like curves (METRONOME on 30); glint flagged 69 microlensing peaks, so the glint flag now also requires the event duration below `max_event_duration_d`. Run 4 on 2,688 simulated supernovae: 1,205 tagged transient-like, secular/RUST/KNELL `insufficient` at HLTDS sampling. Not a sky result |

## Infrastructure (not channels)

| Module | Role | CLI / entry | Workflow | Doc | Outputs |
|---|---|---|---|---|---|
| `alerts.py` | Decides when a human must look: `candidate` / `health` / `milestone` alerts, deduplicated by stable key; frontier-stall and staleness checks read from timestamps *inside* result files | `alert-check` | `alerts.yml` (daily cron; opens issues assigned to the owner) | `alerts.md` | `results/alerts/{latest,frontier,state}.json` |
| `cronwatch.py` | Did the scheduler fire each cron? Reads crons from the workflow files, asks the Actions API, re-dispatches dropped runs | `cron-watch`; `scripts/cron_watch.py` | `cronwatch.yml` (6-hourly cron), `watchdog.yml` | `cronwatch.md` | `results/cronwatch/{status,state}.json` |
| `failsweep.py` | Re-runs failures from the last 24 h, never one whose `head_sha` is no longer its branch head | `scripts/fail_sweep.py` (no CLI subcommand) | `watchdog.yml` | `cronwatch.md` | `results/watchdog/{status,skiplist}.json` |
| `scripts/rubin_outage_check.py` | Asks a second broker whether Rubin's stream or ALeRCE's mirror stopped | script | `rubin-outage.yml` | `rubin-outage.md` | `results/rubin_outage/` |
| `indicators/` | Shared library: multi-axis anomaly scoring consumed by the WD IR-excess run | none | none | none | folded into `results/science/summary.json` |
| `io.py`, `config.py`, `photometry.py`, `population.py`, `pipeline.py`, `report.py`, `figures.py` | Shared helpers: cached VO access, config loading, photometric conversions, the WD-channel pipeline and manuscript figures | — | — | — | — |

## Literature sweeps

Prior-art and full-text sweeps run on the runner via `scripts/*lit*.py` and
commit evidence under `results/<name>lit*/`. They belong to the channel they
were run for:

| Results dir(s) | Channel | Workflow |
|---|---|---|
| `alertlit`, `alertlit2`, `alertlit3` | TOCSIN | `alertlit*.yml` |
| `vnprobelit` … `vnprobelit5` | LOOM | `vnprobelit.yml` |
| `derelictlit` | DERELICT | `derelict.yml` |
| `sextantlit` | SEXTANT | `sextantlit.yml` |
| `emberlit` | EMBER | `ember.yml` |
| `rustlit` | RUST | `rust.yml` |
| `vigillit` | VIGIL | `vigil.yml` |
| `tailingslit` (see its `INTEGRITY.md`: a successful fetch is no evidence the paper is the right one) | TAILINGS | `tailingslit.yml` |
| `chemlit`, `chemlit2`, `chemlit_nist`, `przybylski_lit` | MIDDEN (chemical technosignatures, NIST line data, Przybylski's star) | `chemlit.yml`, `chemlit2.yml`, `chemlit-nist.yml`, `przybylski-lit.yml` |
| `litcheck` | HERDSMAN novelty check (`scripts/litcheck_fetch.py`); `compass.yml` also writes its COMPASS novelty check here (`scripts/compasslit_fetch.py`) | `litcheck.yml`, `compass.yml` |
| `dysonlit`, `litcheck_dyson`, `hephlit`, `seamlit` | Dysonian waste-heat lineage: primary sources, Project Hephaistos dossier, and the structural "seams" an existing search could be evaded through (WD IR excess, CENOTAPH, OSSUARY, VIGIL) | `dysonlit.yml`, `litcheck-dyson.yml`, `hephlit.yml`, `seamlit.yml` |
| `zacklit` | Zackrisson+2018 "SETI with Gaia" prior art for the underluminosity / grey-dimming axis (CENOTAPH leg 1) | `zacklit.yml` |
| `offlit` | Sources that disappear or turn off: prior art for EMBER | `offlit.yml` |
| `disaplit`, `disaplit2`, `disaplit3`, `vanishlit`, `vanishlit2`, `vanishlit3` | Disappearance-class searches, VASCO and vanishing stars: prior art for SHROUD | `disap-lit.yml`, `disap-lit2.yml`, `disap-lit3.yml`, `vanishing-lit.yml`, `vanishing-lit2.yml`, `vanishing-lit3.yml` |
| `necrolit` | Novelty check for the necrosignature taxonomy in `necrosignatures.md` (S-numbered signatures cited by RUST, KNELL, VIGIL, EMBER, SHROUD, CENOTAPH, TOCSIN) | `necrolit.yml` |
| `farir_docs`, `farir_stats`, `catrecon` | AKARI-FIS / IRAS far-IR instrument documentation, measured crossmatch systematics and catalogue metadata (CENOTAPH leg 3, EMBER) | `farir-params.yml`, `catrecon.yml` |
| `survey_recon` | Live probe of what can feed TOCSIN and LOOM while Rubin is dark (`substitute-surveys.md`) | `survey-recon.yml` |
| `bafflelit` | Prior-art sweep for BAFFLE: mid-IR *deficit* searches, zoo/planetarium-hypothesis observational tests, outer-solar-system occulter/parallax searches, radio voids at stars, concealment arguments (`scripts/bafflelit_fetch.py`) | `bafflelit.yml` |
| `romanlit` | Prior-art sweep for the four Roman-only signatures S40–S43 and for any Roman/WFIRST SETI proposal (`scripts/romanlit_fetch.py`) | `roman-lit.yml` |
| `metronomelit`, `lanternlit`, `falloutlit` | Prior-art sweeps for METRONOME, LANTERN and FALLOUT (`scripts/<name>_fetch.py`; run as the `lit` job of each channel's workflow) | `metronome.yml`, `lantern.yml`, `fallout.yml` |
| `necrofrontier_lit` | Prior-art sweep for the eighteen successor-residue signatures S46–S63 of `necrofrontier.md` (`scripts/necrofrontier_fetch.py`): fifteen decoy-aware query groups — isotopic purity, hot swarms, SPHEREx/Euclid stellar lines, DASCH cessation, polluted-WD refinery vectors, shattered cradles, artificial ISM molecules, lunar cold-trap artifacts, the terrestrial record, star-pair beam spillover, the growing transit, ignition, the starspot energy ceiling, post-biological hosts, in-situ grains | `necrofrontier-lit.yml` |
| `necrofrontier` (probe) | Reachability and product check of every data source S46–S63 needs and this repository has never queried — SPHEREx QR2/SPLICES, Euclid Q1, DASCH DR7, the Presolar Grain Database, PEWDD/PyllutedWD, CASSIS, CDMS/JPL, Diviner PCP / Mini-RF / ShadowCam, SGP/EarthChem/GEOROC, the Exoplanet Archive depth tables, Cassini CDA, the superflare tables, Breakthrough Listen open data (`scripts/necrofrontier_probe.py`; verdicts `REACHED_WITH_PRODUCT` / `REACHED_NO_PRODUCT` / `NOT_REACHED` per endpoint and READY / PARTIAL / BLOCKED per signature) | `necrofrontier-probe.yml` |

## Gaps this index exposed

Recorded so the next session does not rediscover them:

- **Absent from `STATUS.md` entirely:** MIDDEN, SEXTANT, HERDSMAN, HERDSMAN-B,
  ISOTHERM, COMPASS and the cronwatch/failsweep infrastructure. Their verdicts
  live only in their `results/*/REPORT.md` or `summary.json` (quoted above).
- **Stale `STATUS.md` row, now corrected:** the channel-state table said
  CENOTAPH was "pending first archive run" while
  `results/cenotaph/summary.json` already carried `closure_but_crowded_beam`
  on a partial sample. The row was updated on 2026-09-01; the general lesson
  is that a committed result and its `STATUS.md` row drift unless the run's
  commit-back step is the thing that edits the row.
- **Built but never run to a result:** OSSUARY and SHROUD have no committed
  `summary.json`; SEXTANT has run only its probe; VIGIL reached no data.
- **Wired but unreachable from any workflow:** `cluster-aggregate`;
  `failsweep` has no CLI subcommand (script-only).
- **No `docs/` page:** `accel`, `cluster`, `crosscorr`, `dimming`, `galactic`,
  `iso`, `jwst_bio`, `lhs1140`, `lhs1140_origin`, `panspermia`, `seti_archive`,
  `spectra`, `xp`. Their design is documented in `STATUS.md` sections and
  module docstrings.
- **Verdict lives outside `summary.json`:** WD IR excess, accel, cluster,
  spectra, midden, herdsman, herdsman_b, compass, galactic, iso, panspermia,
  lhs1140_origin. `alerts.py` only reads the Rubin channels, so none of these
  is covered by the staleness heartbeat.

## Outside the technosignature programme

| Channel | Question | Data | CLI | Workflow | Doc | Results → current verdict |
|---|---|---|---|---|---|---|
| **GOO** (`goo`) | How far and how fast an *uncontrolled* self-replicator ("grey goo") propagates at planet, system, galaxy (passive: radiation-pressure blowout and phase-mixing; active: the inherited probe front) and cosmic scale; the contact graph of every natural mass-transport channel (interstellar objects, impact ejecta, free-floating planets, flybys, dust, supernovae, the birth cluster) and how long the Galaxy takes to be touched by something touched by something else; what residue each leaves and for how long (a detectable dead residue lives ≤ P_orb / f_min); what the published surveys and this repository's channels already constrain; and the Bayesian implication, with and without the anthropic shadow, for the probability that a civilisation at our stage does it to itself (S44, S45) | No archive query — computation from `config/goo.yaml` plus the compiled record (Ĝ III, Hephaistos I/II, Kennedy & Wyatt, this repository's WD / LOOM / DERELICT results); the runner sweep `goolit.yml` verifies every citation and runs the novelty queries | `python -m seti.goo.run --stage {planet,system,passive,active,bayes,contact,survival,figures,numbers,all}` | `goolit.yml` (record fetch + reference verification) | `goo.md` | `results/goo/{planet,system,passive,active,bayes,contact,survival}.json`, `results/goo/figures/`: crust 5 kyr / bulk 1.1 Myr at any doubling time; belt 14 yr; passive branch: landings do not wait for annulus mixing, and above a ~10⁶ yr functional lifetime (the hop to the nearest star) a chain of seedings crosses the Galaxy in 1.6 Gyr at any longer survival, rock-borne devices never (`survival.json`); galaxy 0.6–500 Myr active; prior-free 95 % bound on the escaping-branch probability per civilisation 3×10⁻⁷ at Λ = 100 from the 10⁵-galaxy null, none on the planet-bound branch; the elicited planet-scale accident probability moves by ×0.990 if 1 % of accidents are interstellar. Manuscripts `paper/goo/` and `paper/replicators/`; `results/goolit/` carries the verification |
| **LZEDGE** (`lzedge`) | The LZ 248 keV nuclear-recoil candidate (arXiv:2609.02823) read as a measurement of the fastest local dark matter: joint likelihood of the event's energy *and date* over (m_χ, δ) for sharp / soft / power-law tails, the published Gaia escape speeds integrated over their uncertainties, and an LMC-boosted tail; reachability calendar; cross-experiment seasonal exposure | The record fetched by `lzlit.yml` (paper text, follow-ups, HEPData tables); no archive query — pure computation | `python -m seti.lzedge.run --stage {kinematics,scan,tails,posterior,vesc,figures,numbers,all}` (or `seti lzedge -- …`) | `lzlit.yml` (record fetch) | `lzedge.md` | `results/lzedge/posterior_summary.json`, `vesc_marginal.json`, `predictions.json`: 1 TeV splitting 350 keV (1σ 330–360) under LZ's halo, mode 330–340 keV for every escape-speed measurement, 95 % upper bound 339–394 keV set by the measurement; date factor 2.3 (1 TeV) to 4.3 (500 GeV); the event's likelihood of v_esc flat (±5 %); halo evidences within 2×; draft in `paper/lzedge/` |
