name: vnprobelit3

# Prior-art / novelty adjudication for a POPULATION search for alien
# self-replicating (von Neumann) probes among solar-system objects using
# Rubin/LSST SSO alerts (ephemeris residuals + non-gravitational parameters).
# The sandbox blocks arxiv.org / api.openalex.org; the runner has egress
# (CLAUDE.md acquisition pattern). Results are committed back to the branch.

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  vnprobelit3:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Fetch prior-art evidence (soft 50-min deadline, partials kept)
        run: |
          timeout -k 30 3300 python scripts/vnprobelit3_decisive.py 3000 \
            || echo "fetcher hit soft deadline or errored; committing partials"
          echo "=== files ==="
          ls -la results/vnprobelit3/ || true
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f results/vnprobelit3/ || true
          if git diff --cached --quiet; then
            echo "No result changes to commit."
          else
            git commit -m "vnprobelit3: decisive full text + occupancy matrix for the von Neumann probe population angle"
            git checkout -- . 2>/dev/null || true
            git pull --rebase --autostash origin ${{ github.ref_name }} || true
            git push origin HEAD:${{ github.ref_name }} || \
              { git checkout -- . 2>/dev/null || true; \
                git pull --rebase --autostash origin ${{ github.ref_name }}; \
                git push origin HEAD:${{ github.ref_name }}; }
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: vnprobelit3-outputs
          path: results/vnprobelit3/
          if-no-files-found: warn
