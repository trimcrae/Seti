"""ISOTHERM — the SHAPE of the waste heat in temperature space.

Claim
-----
Every executed technosignature search thresholds on how MUCH infrared excess
there is.  None thresholds on the WIDTH of the dust temperature distribution,
on the emissivity index ``beta`` fitted as a free parameter, or on the presence
of >= 3 discrete temperature components in geometric progression.  Those are
where the engineering lives: an engineered radiator is optimised to one
temperature (S5), and a staged computational cascade gives discrete steps (S6),
whereas circumstellar dust on a radial gradient ``T ~ r**(-1/2)`` is
*necessarily* a broad superposition.

Why it is necessary, not merely novel: the natural warm-debris-disk temperature
locus sits at ~190 K (Morales et al. 2011), directly on top of the Dyson-sphere
range every search targets.  Amplitude and colour cannot separate populations
that overlap in exactly those observables; shape can.

Corpus: CASSIS (~13,000 public Spitzer/IRS 5-38 micron spectra), which has never
been used for a technosignature search.  Four broadband points cannot measure an
emissivity index or a silicate feature.

Prior art and the honest novelty verdict are in ``docs/isotherm.md``.
"""
